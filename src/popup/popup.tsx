// Popup script — minimal for now; just handles the extension popup UI.
// The popup is purely informational (instructions + legend).
// No interactive logic needed yet.

console.log("[Network Highway] Popup loaded");
